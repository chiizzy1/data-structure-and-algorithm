
/*  *****
Challenge: 8kyu  CVR representation

Instructions:  

                                    MySolution
            
              function toCsvText(array) {
                    return array.map((arr, i) => i === array.length - 1 ? arr.join(',') : `${arr.join(',')}\n` ).join('')
                }
                                    CodeWars Solution

            
*/